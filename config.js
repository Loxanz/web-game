/**
 * Optional dedicated WebSocket server URL (Railway / Render).
 * Leave empty to use built-in peer-to-peer multiplayer on Vercel / GitHub Pages.
 *
 * Example:
 *   window.UNO_WS_URL = 'wss://your-app.up.railway.app';
 */
window.UNO_WS_URL = window.UNO_WS_URL || '';
